name: kazpost_tracker
description: Kazpost tracker

version: 3.0.1+3000001

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Service locator
  get_it: ^4.0.1
  # Bloc for state management
  flutter_bloc: ^3.2.0
  # Value equality
  equatable: ^1.1.1
  # Functional programming thingies
  dartz: ^0.8.9
  # Remote API
  data_connection_checker: ^0.3.4
  http: ^0.12.0+4
  # Local cache
  shared_preferences: ^0.5.6+3

  sembast: ^1.15.2
  path_provider: ^0.5.0+1
  intl: ^0.16.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - images/